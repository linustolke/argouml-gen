For some reason, git is needed in the docker image together with maven.
This is available for some of the prepared maven images but not all.

This directory is a job that creates new docker images to fix this.
